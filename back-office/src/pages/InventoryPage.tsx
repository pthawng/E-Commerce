import { ProductTable } from '@/widgets/inventory/ProductTable';

export const InventoryPage = () => {
    return (
        <div className="max-w-7xl mx-auto">
            <ProductTable />
        </div>
    );
};

export default InventoryPage;
