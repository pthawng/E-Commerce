/**
 * Auth Types
 * Re-export từ @shared và định nghĩa types riêng cho auth feature
 */

export type { User, AuthTokens, LoginPayload, RegisterPayload, AuthResponse } from '@shared';

// Thêm types riêng cho auth feature nếu cần
// export interface AuthFormValues {
//   email: string;
//   password: string;
// }

