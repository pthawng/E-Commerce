{{!-- verify-email.hbs --}}
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Verify your email</title>
    <style>
      /* Simple, safe inline-friendly styles (email clients strip external CSS) */
      body { margin:0; padding:0; background-color:#f4f6f8; font-family: Arial, Helvetica, sans-serif; }
      .wrapper { width:100%; table-layout:fixed; background-color:#f4f6f8; padding: 20px 0; }
      .main { background:#ffffff; width:100%; max-width:600px; margin:0 auto; border-radius:8px; overflow:hidden; }
      .spacer { height:24px; }
      .content { padding:24px; color:#333333; line-height:1.5; font-size:16px; }
      .logo { padding:20px 24px 0 24px; text-align:left; }
      .btn { display:inline-block; text-decoration:none; padding:12px 20px; border-radius:6px; background:#2563eb; color:#ffffff; font-weight:600; }
      .muted { color:#6b7280; font-size:14px; }
      .footer { padding:18px 24px; font-size:13px; color:#9ca3af; text-align:center; }
      @media screen and (max-width:420px) {
        .content { padding:16px; font-size:15px; }
        .logo { padding-left:16px; padding-right:16px; }
      }
    </style>
  </head>
  <body>
    <center class="wrapper">
      <table class="main" role="presentation" cellpadding="0" cellspacing="0">
        <tr>
          <td class="logo">
            {{#if logoUrl}}
              <img src="{{logoUrl}}" alt="{{companyName}}" width="140" style="display:block; border:0;"/>
            {{else}}
              <strong style="font-size:20px">{{companyName}}</strong>
            {{/if}}
          </td>
        </tr>

        <tr><td class="spacer"></td></tr>

        <tr>
          <td class="content">
            <h2 style="margin:0 0 8px 0; font-size:22px;">Xác minh email của bạn</h2>

            <p style="margin:0 0 16px 0;">
              Chào {{#if name}}{{name}}{{else}}bạn{{/if}},<br/>
              Cám ơn bạn đã đăng ký với <strong>{{companyName}}</strong>. Vui lòng bấm nút dưới đây để xác minh địa chỉ email của bạn.
            </p>

            <p style="text-align:center; margin:20px 0;">
              <a href="{{verificationUrl}}" class="btn" target="_blank" rel="noopener noreferrer">
                Xác minh email
              </a>
            </p>

            <p style="margin:0 0 12px 0;" class="muted">
              Nếu nút trên không hoạt động, sao chép và dán đường link sau vào trình duyệt của bạn:
            </p>
            <p style="word-break:break-all; font-size:13px; color:#374151; margin:0 0 18px 0;">
              <a href="{{verificationUrl}}" style="color:#2563eb; text-decoration:underline;" target="_blank" rel="noopener noreferrer">{{verificationUrl}}</a>
            </p>

            {{#if expiryMinutes}}
              <p class="muted" style="margin:0 0 12px 0;">
                Lưu ý: liên kết sẽ hết hạn sau <strong>{{expiryMinutes}} phút</strong>.
              </p>
            {{/if}}

            <hr style="border:none; border-top:1px solid #eef2f7; margin:18px 0;"/>

            <p style="margin:0 0 6px 0;">
              Nếu bạn không yêu cầu xác minh này, bạn có thể bỏ qua email này — không có thay đổi nào được thực hiện trong tài khoản.
            </p>

            <p style="margin:12px 0 0 0; color:#6b7280; font-size:14px;">
              Trân trọng,<br/>
              Đội ngũ {{companyName}}
            </p>
          </td>
        </tr>

        <tr><td class="spacer"></td></tr>

        <tr>
          <td class="footer">
            <div>
              Bạn cần hỗ trợ? Gửi email tới <a href="mailto:{{supportEmail}}" style="color:#6b7280; text-decoration:underline;">{{supportEmail}}</a>
            </div>
            <div style="margin-top:8px;">
              © {{companyName}} — All rights reserved.
            </div>
          </td>
        </tr>
      </table>
    </center>
  </body>
</html>
